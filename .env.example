# Backend (FastAPI)
GEMINI_API_KEY=            # required for AI readings + palmistry (chart math needs NO key)
GEMINI_TEXT_MODEL=gemini-2.5-flash

# Frontend (Next.js)
JYOTISH_API_URL=http://localhost:8000
